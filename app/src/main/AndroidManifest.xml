<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.capitals2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EuropeActivity"
            android:label="@string/europe_activity_name" />
        <activity
            android:name=".AsiaActivity"
            android:label="@string/asia_activity_name" />
        <activity
            android:name=".AfricaActivity"
            android:label="@string/africa_activity_name" />
        <activity
            android:name=".AustraliaActivity"
            android:label="@string/australia_activity_name" />
        <activity
            android:name=".NorthAmericaActivity"
            android:label="@string/north_america_activity_name" />
        <activity
            android:name=".SouthAmericaActivity"
            android:label="@string/south_america_activity_name" />
        <activity
            android:name=".AntarcticActivity"
            android:label="@string/antarctic_activity_name"></activity>
    </application>

</manifest>